<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_clear_list"
          android:title="@string/clearListMenu"
          app:showAsAction="never" />
    
    <item android:id="@+id/menu_clear_device_cache"
          android:title="@string/ClearDeviceCacheMenu"
          app:showAsAction="never" />
    <!--

    <item
        android:id="@+id/menu_start_scan"
        app:showAsAction="ifRoom"
        android:title="@string/startScanMenu"
        android:icon="@android:drawable/ic_menu_search"/>

    <item
        android:id="@+id/menu_stop_scan"
        android:orderInCategory="2"
        app:showAsAction="ifRoom"
        android:title="@string/StopScanMenu"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"/>


    <item android:id="@+id/menu_add_node_emulator"
        android:title="@string/AddNodeEmulator"
        app:showAsAction="never" />
    menu_add_node_emulator
    -->
</menu>
